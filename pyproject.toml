[project]
name = "quanttide-example-of-knowledge-engineering"
version = "0.1.0"
description = "量潮知识工程示例项目"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "neo4j",
    "python-dotenv",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
]
